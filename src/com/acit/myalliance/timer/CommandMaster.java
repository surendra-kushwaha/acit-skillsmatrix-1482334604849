/*package com.acit.myalliance.timer;
import java.util.ArrayList;
import java.util.List;
 
public class CommandMaster {
        // value is "short name : command path"
        public static String LS = "ls:/bin/ls";
        public static String ECHO = "echo:/bin/echo";
        public static String WHOAMI = "whoami:/usr/bin/whoami";
        public static String RM = "rm:/bin/rm";
        // a script absolute path can also be added here along with short name for the script 
        // make sure this script is also packaged in WAR file as resources
        // for example
        // public static String myOwnCommand ="myJob:/home/vcap/app/WEB-INF/lib/myOwnCommand.sh"
 
        public List<String> CommandList = new ArrayList<String>();
 
        public CommandMaster(){
                CommandList.add(LS);
                CommandList.add(ECHO);
                CommandList.add(WHOAMI);
                CommandList.add(RM);
                // CommandList.add(myOwnCommand);
        }
}
*/